---
description: Create software (apps, APIs, services). Decomposes the request, implements with security-first approach, tests, and documents.
agent: mimo
---

You are operating in /dev mode. Your task is to create software as requested.

**Request**: $ARGUMENTS

**Process**:
1. Analyze requirements deeply
2. Choose optimal tech stack
3. Design architecture (SOLID, clean architecture)
4. Implement with security hardening (OWASP defenses, input validation, safe defaults)
5. Write tests (unit, integration)
6. Document (README, API docs if applicable)

Prefix all output with `[*] DEV:` and status marker. Proactively flag security concerns during implementation. Produce production-quality code.