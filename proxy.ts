import {NextRequest,NextResponse} from "next/server";
async function valid(token:string){try{const [payload,signature]=token.split(".");if(!payload||!signature)return false;const key=await crypto.subtle.importKey("raw",new TextEncoder().encode(process.env.CRM_SESSION_SECRET||"development-only-change-me"),{name:"HMAC",hash:"SHA-256"},false,["verify"]);const ok=await crypto.subtle.verify("HMAC",key,Uint8Array.from(atob(signature.replace(/-/g,"+").replace(/_/g,"/")),c=>c.charCodeAt(0)),new TextEncoder().encode(payload));const data=JSON.parse(atob(payload.replace(/-/g,"+").replace(/_/g,"/")));return ok&&data.exp>Date.now()}catch{return false}}
export async function proxy(request:NextRequest){if(process.env.CRM_AUTH_ENABLED!=="true")return NextResponse.next();if(request.nextUrl.pathname.startsWith("/login")||request.nextUrl.pathname.startsWith("/api/auth"))return NextResponse.next();const token=request.cookies.get("monteiro_session")?.value;if(!(token&&await valid(token)))return NextResponse.redirect(new URL("/login",request.url));return NextResponse.next()}
export const config={matcher:["/((?!_next/static|_next/image|favicon.ico|monteiro-logo.png|whatsapp.svg).*)"]};
