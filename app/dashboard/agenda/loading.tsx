// DNA CRM — RC1 — Loading skeleton da agenda
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function AgendaLoading() {
  return <SectionSkeleton title="Agenda" rows={6} />
}