// DNA CRM — RC1 — Loading skeleton operacao
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function OperacaoLoading() {
  return <SectionSkeleton title="Centro de Operação" rows={9} />
}