// DNA CRM — RC1 — Loading skeleton gestão
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function GestaoLoading() {
  return <SectionSkeleton title="Gestão Comercial" rows={8} />
}