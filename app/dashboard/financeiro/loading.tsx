// DNA CRM — RC1 — Loading skeleton financeiro
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function FinanceiroLoading() {
  return <SectionSkeleton title="Financeiro" rows={6} />
}