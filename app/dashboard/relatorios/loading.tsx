// DNA CRM — RC1 — Loading skeleton relatorios
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function RelatoriosLoading() {
  return <SectionSkeleton title="Relatórios" rows={5} />
}