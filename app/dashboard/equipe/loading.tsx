// DNA CRM — RC1 — Loading skeleton equipe
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function EquipeLoading() {
  return <SectionSkeleton title="Equipe" rows={5} />
}