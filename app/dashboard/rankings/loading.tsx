// DNA CRM — RC1 — Loading skeleton rankings
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function RankingsLoading() {
  return <SectionSkeleton title="Rankings" rows={5} />
}