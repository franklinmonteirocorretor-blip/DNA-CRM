// DNA CRM — RC2 — Loading skeleton novo cliente
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function NovoClienteLoading() {
  return <SectionSkeleton title="Novo Cliente" rows={6} />
}