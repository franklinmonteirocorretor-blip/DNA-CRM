// DNA CRM — RC2 — Loading skeleton detalhe cliente
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function ClienteDetalheLoading() {
  return <SectionSkeleton title="Detalhes do Cliente" rows={10} />
}