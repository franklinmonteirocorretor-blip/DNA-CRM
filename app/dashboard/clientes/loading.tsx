// DNA CRM — RC1 — Loading skeleton clientes
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function ClientesLoading() {
  return <SectionSkeleton title="Clientes" rows={8} />
}