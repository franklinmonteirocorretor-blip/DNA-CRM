// DNA CRM — RC1 — Loading skeleton documentos
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function DocumentosLoading() {
  return <SectionSkeleton title="Documentos" rows={5} />
}