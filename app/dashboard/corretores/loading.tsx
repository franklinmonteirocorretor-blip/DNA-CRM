// DNA CRM — RC1 — Loading skeleton corretores
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function CorretoresLoading() {
  return <SectionSkeleton title="Corretores" rows={5} />
}