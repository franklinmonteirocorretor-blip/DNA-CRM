// DNA CRM — RC1 — Loading skeleton followup
import SectionSkeleton from '@/src/components/skeletons/SectionSkeleton'

export default function FollowupLoading() {
  return <SectionSkeleton title="Follow-up" rows={5} />
}